name: Encoding Check

on:
  push:
  pull_request:

jobs:
  check-encoding:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Check broken UTF-8 encoding
        run: |
          echo "🔎 Verificando caracteres quebrados..."
          if grep -R "mÃ\|Ã§\|Ã£\|Ã©\|â€“\|â€™\|â€œ\|â€\|Â©\|Â\|ðŸ\|ï¸" .; then
            echo "❌ ERRO: Texto com encoding quebrado encontrado"
            exit 1
          else
            echo "✅ Encoding OK"
          fi
